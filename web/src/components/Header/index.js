import React from "react";
// import { Link } from "react-router-dom";
import "./style.css";

const Header = () => (
  <div id="header">
    <div className="greeting">Gopher Mail!</div>
    <div className="nav">Sign Out!</div>
  </div>
);

export default Header;
